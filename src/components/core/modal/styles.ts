import { StyleSheet } from 'aphrodite'

export const styles = StyleSheet.create({
  root: {
    position: 'fixed',
    left: 0,
    top: 0,
    bottom: 0,
    right: 0,
    // backgroundColor: 'rgba(0,0,0,.2)',
    color: '##FFF',
    fontSize: '40px',
  },
})
